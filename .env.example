# Canonical site URL (used for sitemap, Open Graph, structured data).
# On Netlify, set this to your production domain. If unset, Netlify's built-in URL is used.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
